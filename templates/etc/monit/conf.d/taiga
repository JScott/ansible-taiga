check process taiga with pidfile {{ taiga_front_pidfile_path }}
  start program "sudo gulp --cwd {{ taiga_front_install_path }}"
  stop program "sudo pkill -f gulp"
  if 5 restarts within 5 cycles then timeout
