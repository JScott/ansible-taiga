check process taiga matching "gunicorn"
  start program "/usr/local/bin/gunicorn -w 3 -t 60 --pythonpath={{ taiga_back_install_path }} -b 0.0.0.0:8001 taiga.wsgi"
  stop program "/usr/bin/pkill -f gunicorn"
  if 5 restarts within 5 cycles then timeout
